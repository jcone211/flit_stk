# README 纯文字风格实现计划

> **面向 AI 代理的工作者：** 必需子技能：使用 superpowers:subagent-driven-development（推荐）或 superpowers:executing-plans 逐任务实现此计划。步骤使用复选框（`- [ ]`）语法来跟踪进度。

**目标：** 将 README 的装饰性 emoji 替换为克制的纯文字风格，同时保留现有功能说明、项目图标、徽章和演示资源。

**架构：** 只修改 `README.md` 的 Markdown/HTML 表达，不改变项目代码、图片资源或扩展功能。顶部保留居中项目图标和徽章，章节与功能项改用简洁中文或专业英文标签，并同步更新目录锚点。

**技术栈：** Markdown、HTML；使用 Git 进行版本管理。

---

### 任务 1：替换 README 头部和导航标题

**文件：**
- 修改：`README.md:1-45`

- [ ] **步骤 1：移除头部装饰性 emoji**

将包含 `✨`、`🚀`、`⚡` 的章节标题改为：

```markdown
## 核心能力

## 快速开始

## 快速实操（导入预置数据试玩）

## 详细文档
```

保留项目图标、标题、简介、版本号、徽章以及“你为什么需要 flit stk？”和两条痛点内容。

- [ ] **步骤 2：同步顶部目录链接**

将目录区域改为：

```html
<p align="center">
  <a href="#核心能力">功能</a> ·
  <a href="#快速开始">快速开始</a> ·
  <a href="#快速实操导入预置数据试玩">快速实操</a> ·
  <a href="#详细文档">详细文档</a>
</p>
```

GitHub 对中文标题的锚点会去掉标点并保留中文字符，因此“快速实操（导入预置数据试玩）”对应 `#快速实操导入预置数据试玩`。

- [ ] **步骤 3：检查头部 HTML 闭合**

确认项目图标、简介、痛点、版本信息和目录区域的每个 `<p>` 标签均正确闭合，并确保头像图片路径仍为 `icons/icon128.png`。

### 任务 2：替换功能表格中的装饰性图标

**文件：**
- 修改：`README.md` 的“核心能力”功能表格

- [ ] **步骤 1：使用专业短标签替换功能名称**

将功能表格的第一列调整为：

```markdown
| 功能 | 说明 |
| --- | --- |
| **AI Agent** | 自然语言提问操作股票、组合、要点，持久记忆偏好，多 API Key 配置，可结合小石大数据做量化扩展 |
| **多渠道实时行情** | 集成 **adata 免费实时数据**、**小石大数据**、**问财 & 雪球页面爬取** 三种渠道，全局设置一键切换 |
| **多条件提醒** | 分别按**当日涨跌幅**与**导入以来涨跌幅**设置阈值，实时监控并触发提醒 |
| **要点与事件记录** | 记录交易逻辑与预测事件，追踪准确率，分析结合实际动态调整策略 |
| **专业免费渠道** | 批量打开问财/雪球看 K 线，一键批量导入到指定组合 |
| **全局可控** | 按需启用/关闭各功能模块，不用的不占空间 |
| **一键迁移** | 导出/导入完整数据，快速无缝迁移到其他设备 |
```

保留原有七项能力、顺序和说明，仅移除 `🤖`、`📡`、`🔔`、`📝`、`🔗`、`⚙️`、`📦`。

- [ ] **步骤 2：移除演示图说明中的装饰性表达**

保持两张 GIF 的路径和说明不变，确认“AI分析演示”和“一键导入演示”只作为图片 alt 文本使用，不新增 emoji。

### 任务 3：验证并提交 README 更新

**文件：**
- 测试：`README.md`

- [ ] **步骤 1：检查装饰性 emoji 是否清除**

运行：

```powershell
rg "✨|🤖|🚀|⚡|📡|🔔|📝|🔗|⚙️|📦" README.md
```

预期：无匹配输出。项目图标文件名中的内容不属于本项检查范围。

- [ ] **步骤 2：检查关键资源和链接仍存在**

运行：

```powershell
rg "icons/icon128\.png|images/README/ai分析\.gif|images/README/一键导入\.gif|thswc_full_backup_20260829-231116\.json|README_old\.md" README.md
```

预期：五个资源或链接均能匹配到。

- [ ] **步骤 3：检查 Markdown 差异**

运行：

```powershell
git diff --check
git diff -- README.md
```

预期：`git diff --check` 无错误；差异仅涉及 README 文案、标题和表格标签。

- [ ] **步骤 4：提交变更**

```powershell
git add README.md
git commit -m "docs: simplify README visual language"
```
